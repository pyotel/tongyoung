import React from "react";
import ReactDOM from "react-dom/client";
import OvertimeCalc from "../overtime-calculator.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OvertimeCalc />
  </React.StrictMode>
);
